#include "main.h"
/**
 * main - Entry point
 * postitive_or_negative: determines the status of an input
 * Return: Always 0 if successful
 */
int main(void)
{
	int i = 0;

	postitive_or_negative(i)
	return (0);
}

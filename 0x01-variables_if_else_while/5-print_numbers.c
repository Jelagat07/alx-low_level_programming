#include <stdio.h>
/**
 * main - print all single digit numbers of base 10
 * starting from zero followed by a newline
 * Return: Always 0-success
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%d", i);
	printf("\n");
	return (0);
}

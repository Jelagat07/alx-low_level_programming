#include "main.h"
/**
 * _abs - prints the absolute value
 * Return: Always a positive integer
 * @k: an integer
 */
int _abs(int k)
{
	int r;

	if (k >= 0)
	{(r = k * 1);
	return (r);
	}
	else
	{(r = k * -1);
	return (r);
	}
}
